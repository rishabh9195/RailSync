function simRender(){let t=RailETA.getTrain('12301');simEta.textContent=t.eta;affected.innerHTML=RailETA.getTrains().map(t=>`<div class="side-event"><i class="event-dot ${t.risk==='High'?'red':''}"></i><div><b>${t.id} ${t.name}</b><br><small class="muted">${t.location} · predicted delay +${t.predictedDelay} min · ${t.risk} risk</small></div></div>`).join('')}document.addEventListener('DOMContentLoaded',()=>{simRender();document.querySelectorAll('[data-s]').forEach(b=>b.onclick=()=>{document.querySelectorAll('[data-s]').forEach(x=>x.classList.remove('active'));b.classList.add('active');simText.textContent='🔄 Recalculating network impact…';setTimeout(()=>{let r=RailETA.simulateScenario(b.dataset.s);simImpact.textContent=(r.impact>=0?'+':'')+r.impact+' min';simText.textContent=`Scenario applied. Passenger ETA and alerts have been updated.`;simRender();UI.toast('✓ Simulation result applied')},500)})})
