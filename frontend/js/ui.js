const UI={nav(type='passenger'){let links=type==='operator'?[['operator.html','Overview'],['operator.html#network','Live Network'],['operator.html#trains','Trains'],['events.html','Events'],['analytics.html','Predictions'],['analytics.html','Analytics'],['simulation.html','Simulation']]:[['index.html','Home'],['passenger.html','Track Train'],['schedule.html','Train Calendar'],['alerts.html','Alerts']];return `<nav class="nav ${type==='operator'?'operator-nav':''}"><a class="brand" href="index.html">Rail<b>Sync</b></a><div class="navlinks">${links.map(([u,n])=>`<a href="${u}">${n}</a>`).join('')}</div><a class="btn small ${type==='operator'?'secondary':'dark'}" href="${type==='operator'?'passenger.html':'operator.html'}">${type==='operator'?'Passenger View':'Switch to Operator'} <i class="fa-solid fa-arrow-right"></i></a></nav>`},footer(){return `<footer class="footer"><b>Prototype Demonstration</b><br>Railway running information, weather, operational events and prediction values shown in this prototype are simulated demonstration data, not an official Indian Railways live feed.</footer>`},toast(msg){let wrap=document.querySelector('.toast-wrap')||document.body.appendChild(Object.assign(document.createElement('div'),{className:'toast-wrap'}));let t=document.createElement('div');t.className='toast';t.textContent=msg;wrap.append(t);setTimeout(()=>t.remove(),3500)},modal(id,content){let el=document.getElementById(id);if(!el){el=document.createElement('div');el.id=id;el.className='modal-back';document.body.append(el)}el.innerHTML=`<div class="modal" role="dialog" aria-modal="true">${content}</div>`;el.classList.add('open');el.addEventListener('click',e=>{if(e.target===el||e.target.matches('[data-close]'))el.classList.remove('open')})},risk(r){return `<span class="tag ${r==='High'?'red':r==='Low'?'green':'amber'}">${r.toUpperCase()}</span>`},formatTrain(t){return `<b>${t.id} ${t.name}</b>`}};document.addEventListener('DOMContentLoaded',()=>{document.body.insertAdjacentHTML('afterbegin',UI.nav(document.body.dataset.view));document.body.insertAdjacentHTML('beforeend',UI.footer());const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('in-view');observer.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('main .card').forEach((card,index)=>{card.classList.add('scroll-reveal');card.style.transitionDelay=Math.min(index%5,4)*35+'ms';observer.observe(card)})});
